# Core dependencies (always installed)
arxiv>=2.1.0
anthropic>=0.40.0
rich>=13.0.0
click>=8.1.0
jinja2>=3.1.0
requests>=2.31.0
pyyaml>=6.0
python-dateutil>=2.8.0\nmarkdown>=3.5

# Optional — semantic search (pip install arxiv-autopilot[semantic-search])
# sentence-transformers>=2.2.0

# Optional — web dashboard (pip install arxiv-autopilot[web])
# fastapi>=0.110.0
# uvicorn>=0.29.0
# python-multipart>=0.0.9

# Optional — terminal UI (pip install arxiv-autopilot[tui])
# textual>=0.50.0

# Optional — background scheduler (pip install arxiv-autopilot[scheduler])
# apscheduler>=3.10.0

# Install everything: pip install arxiv-autopilot[all]
